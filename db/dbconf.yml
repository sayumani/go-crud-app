local:
  driver: postgres
  open: host='$DB_HOST' port='$DB_PORT' user='$DB_USERNAME' password='$DB_PASSWORD' dbname='$DB_NAME' sslmode=disable
production:
  driver: postgres
  open: host='$DB_HOST' port='$DB_PORT' user='$DB_USERNAME' password='$DB_PASSWORD' dbname='$DB_NAME' sslmode=disable
